pipeline {
    agent any 
    stages {
        stage('Start') {
            steps {
                sh '''
                    echo "Displaying Environment:"
                    env | sort
                    pwd
                    ls -al
                '''
            }
        }
    }
}