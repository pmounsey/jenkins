pipeline {
    agent any 
    stages {
        stage('Start') {
            steps {
                sh "Echo Starting"
                sh "pwd"
                sh "ls -al"
            }
        }
    }
}